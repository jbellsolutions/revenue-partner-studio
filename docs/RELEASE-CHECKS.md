# v0.2.0-beta.2 — support and verification

This document separates implemented behavior, automated coverage and live acceptance. The public demo is simulated; no demo action proves a model or computer connection.

## Supported scope

Private browser workspace; existing Orgo Linux computers, starting with 8 GB; native Hermes profiles/skills/providers; explicit computer scoping; persistent tasks and specialists; managed cloud screens; reviewed imports; directional trusted connections; optional Mac Hermes chat and files. Mac desktop control is hidden. There is no shared SaaS tenant layer, funded public demo or notarized public desktop download.

## Evidence status

| Check | Current status |
| --- | --- |
| Existing connected private workflow | Prior backend acceptance includes real tool tasks, profile-scoped model changes, handoff/grant checks and four separate managed screens. Private records are excluded from this repository. |
| Public release source, branding and installer | Previous beta.1 [hosted verification](https://github.com/jbellsolutions/revenue-partner-studio/actions/runs/34809435709) passed. Round two local checks are recorded below; match the prerelease commit to its [hosted verification run](https://github.com/jbellsolutions/revenue-partner-studio/actions/workflows/studio-ci.yml). |
| Independent recipient installation | **Not yet verified.** Requires its own authorized accounts/resources, provider authentication, real chat/tools and screen/reopen checks. A preflight or mocked adapter does not satisfy this gate. |
| Browser visual acceptance | **Unverified while administrator-policy verification is unavailable.** This affects live appearance, mobile interaction, keyboard navigation and visible takeover acceptance. No bypass is permitted. |
| 24-hour backend reliability trial | The original trial runs from September 14, 03:45:36 UTC to September 15, 03:45:36 UTC on its original runtime revision. At 9.47 elapsed hours on September 14, all five computers were online. The checkpoint had 12 verified tool jobs, 3,397 successful status samples and 11 failed status samples. Agent/task listings succeeded 3,397 times each. Failures recovered; their cause remains unresolved. The trial has not been reset or declared passed. Interface deployment does not qualify the new computer extension. |
| Public anonymous access, DNS and HTTPS | **Verified September 14, 2026.** The website and private app are live on their separate product domains through the existing Railway service, with DNS retained at Hostinger. All three domain certificates are valid. Anonymous pages/downloads, canonical redirect, private login and computer event streams passed. Cloudflare Pages is retained as an alternate deployment. See the final domain checkpoint below. |

## Performance and reliability targets

Healthy, already-running computers: Studio ready ≤2 s, cached switch ≤250 ms, task acknowledgment ≤1 s, warm screen ≤3 s, reconnect ≤5 s, measured at p95. Model response/tool completion are separate. Unmeasured targets are not guarantees. A complete release acceptance includes 24 hours of repeated switching, simultaneous work, reconnects and browser closure, plus restoration verification.

## Previous beta.1 validation log

Local checks on September 14, 2026:

- Gateway: 28 tests passed; cloud TypeScript check and production build passed.
- Runtime/provider/coordination/import/file/backup suites: 249 tests passed across 21 files.
- Distribution, upstream, Mac workspace and subscription-worker suites: 43 tests passed across 4 files.
- Resumable setup and credential configuration: 8 tests passed, including uncertain creation, resume, destination isolation, missing credentials and accepted-task reconciliation.
- Public demo: 5 automated fixture/static checks passed; production build passed. These are not browser interaction or visual tests.
- Desktop branding, policy and connections: 137 tests passed across 18 files; typecheck and local Bot package build passed. Signature verification passed and the original application identifier is retained. No app was installed or replaced as part of this check.
- Preparation Word guide: rendered and visually checked across both pages. No clipping or overlap found.
- Fresh source scan: 819 findings match previously reviewed exact baseline lines (synthetic fixtures/examples, public OAuth client IDs, C macros and checksums); no new unreviewed finding. Exceptions are bound to full file hashes. Extracted source and runtime packages also passed with no new findings. Final published downloads carry SHA-256 checksums.

Hosted verification passed on Linux, including clean cloud/site installs, TypeScript checks, builds, desktop policy/connection tests and runtime/provider/installer checks. The initial cloud lockfile failure was repaired and verified from a fresh install.

Remaining gates: independent recipient installation, anonymous website/download verification, custom-domain DNS/HTTPS activation, permitted browser acceptance, completed reliability review and restoration acceptance. Detailed private records, account identifiers and personal paths are excluded.

## Round two validation log

Local automated checks on September 14, 2026:

- Cloud: 35 tests passed, including four simulated DOM/component tests of connected-model search, manual selection, activation errors, stale responses and catalog isolation. TypeScript check and production build passed.
- Runtime: 76 tests passed across nine affected model, import, profile, connector, coordination and screen files. Skill-only coverage includes an archive round trip, approved destination identity, scope mismatch, source/destination links, excluded secrets, busy agents, hash verification, updates and conflicts.
- Public site: five fixture checks and the production build passed after preparation downloads were regenerated. Setup/configuration: eight tests passed. The demo imports only shared presentation components and fixture data.
- Clean independent source copy: cloud and site dependency installs, typechecks, builds and demo fixture tests passed. This is build reproducibility, not a fresh customer account installation.
- Desktop compatibility: 18 focused product/connection/model tests and TypeScript checks passed; a local Bot package built and retained its original application ID. No app was installed, replaced or published as a notarized download.
- Preparation guide: both regenerated Word pages were rendered and visually checked.
- Source scan: all 819 existing baseline findings in the extracted snapshot matched reviewed file hashes; no new finding.
- Account/domain state: Railway's custom app domain is attached to the existing service, pending DNS and certificate validation. Cloudflare dashboard access was blocked by the browser policy check; the existing publishing token cannot create zones or edit DNS. No nameserver or existing DNS record was changed.

Hosted verification and final package scans are recorded with the prerelease. Pending before full acceptance: compatible extension promotion and live selected-skill transfer, permitted browser acceptance (including keyboard, scroll, pointer, takeover and switching), measured end-to-end performance, DNS/HTTPS activation, and the unchanged reliability trial. The existing private workspace and rollback clients remain in place.

## Screen and local recovery implementation — September 14, 2026

- Cloud: 41 tests passed; TypeScript check and production build passed. Coverage includes stale connection replay/epochs, retained drafts, screen ownership errors, scoped settings and repair-job identity. These are simulated component/protocol checks, not permitted browser acceptance.
- Runtime: 86 distinct affected tests passed across ten files (screens, profiles, coordination, recovery, connector, updater, imports, skills, workspace and screen bridge). Setup/configuration: eight tests passed. Import unit tests use controlled disk availability and explicitly test low-storage refusal; live disk constraints still apply.
- Installed Mac compatibility passed against the existing native Hermes. The guarded canary initially failed startup and restored its prior source. After disposable Studio build copies were removed and the two Studio service definitions were set to interactive scheduling, the authenticated runtime readiness check passed. Backups and the original native installation remain.
- Live Mac backend: all 20 existing profiles were present. Reopening an existing saved test conversation retained six messages and reused its runtime. A controlled file-read task acknowledged in 431 ms, completed in about 7.3 seconds, and recorded one tool start and one tool result using that profile’s existing subscription. This is one observation, not a p95 measurement or browser/sleep-wake acceptance.
- The original reliability trial is preserved. At 13.45 elapsed hours it had 15 verified tool jobs, 4,825 successful status samples and 11 failed status samples. Agent/task listing samples succeeded 4,825 times each. The failures’ cause remains unresolved. The Mac repair is separately identified; these numbers do not qualify the repaired version or its screens.
- Private Railway database and matching encryption-key backups were created; both SQLite backups passed integrity checks before the interface deployment.

Still pending: authenticated inspection and canary promotion of the affected Orgo computers; actual Brent/GTM assistants, Revenue Agent and Team Support screen/tool assignments; four simultaneous screens and a queued fifth; ownership recovery, human takeover/resume, screen reconnect and cross-screen input checks; higher capacity/headroom qualification; live selected-skill transfer/update/conflict preservation; live reasoning/tool-permission activation and directional/revoked handoff checks on this revision; browser reopening/sleep-wake acceptance; end-to-end p95 measurements; a separate 24-hour repaired-version trial. The browser-policy restrictions remain in force. Source coverage and Mac backend success do not replace those gates.

### Final Mac recovery follow-up

The published interface commit `07bc307` deployed successfully to the existing Railway service; [hosted verification passed](https://github.com/jbellsolutions/revenue-partner-studio/actions/runs/34874084933). A subsequent Mac-specific check exposed Hermes’s 20-second disconnected-client expiry. The `screens-recovery-2` extension now uses native session activation both when reopening a conversation and when its connector reconnects; it does not resubmit accepted tasks.

Live follow-up: a controlled task completed with recorded tool evidence across a connector restart. The final reconnect observation took 758 ms and retained the same runtime and all 16 prior messages beyond the expiry window. A changed-field Advanced turn-limit save succeeded, a stale save was rejected, another profile’s version stayed unchanged, and the next real file-tool task completed. The original limit was restored. These remain individual backend observations, not browser or p95 acceptance.

The repair download now uses the guarded updater for an existing Mac and checks native compatibility in a temporary home. A healthy Studio pairing is reused; a failed cloud pairing may be renewed only with the owner-scoped repair credential for that same saved connection. Busy work, failed updates and mismatched bindings retain their protection. Final focused follow-up: 32 runtime/updater/connector tests passed, bringing distinct affected runtime coverage to 93 tests; setup/configuration tests passed again.

All 11 original trial status failures were traced to the GTM computer. Their timestamps are preserved privately; that computer still requires authenticated inspection to establish the cause. No replacement repaired-version 24-hour window has been started because Orgo screen acceptance and promotion remain pending. Low Mac disk headroom remains an operational limitation for full imports and backup-heavy first installation; production storage guards were preserved.

### Authenticated Orgo repair follow-up

After the owner completed the Tailscale account check, the canary and GTM passed idle-queue and exact-service-owner preflight. Each received the reviewed Studio-only extension with verified SQLite/source/configuration backups and authenticated runtime readiness. Native Hermes and desktop processes were retained. GTM's Team Support, GTM team and assistant had live screen services without ownership records; verified in-place reconciliation restored those records. Revenue Agent's existing ownership was also verified. All four assigned specialists report ready; this does not yet prove their browser tools or visible takeover.

The canary completed a real file-tool task with one recorded tool start/result and a 467 ms acknowledgment. Twenty authenticated screen protocol samples had p95 2,100 ms. Four viewers connected simultaneously to distinct screens and a fifth profile queued at position one. These checks requested neither pixels nor desktop input; browser acceptance remains pending. The original trial's evidence is retained, and the extension promotion marks a runtime transition within that window, not proof of the original or repaired revision completing 24 hours.

Follow-up automated checks: 43 cloud tests, TypeScript and production build passed; affected runtime coverage now totals 98 distinct tests. New checks cover safe ownership adoption/refusal, legacy service-path migration/rollback, preparation of a repair download while its Mac is online, and replacement repair-job tracking. Existing connection credentials remain valid when a repair download is prepared.

GTM also passed 20 authenticated transport samples at p95 1,928 ms, four simultaneous viewers and a queued fifth. Its two team profiles retain generic-browser and other tool restrictions; an explicit managed-screen grant can enable only their assigned screen. Four runtime-adapter tests and three permission tests passed, and coordination regressions remained green. Live tool activation for those profiles is tracked separately from their ready screen transports.

### Real screen tools and reviewed skills

Live GTM interaction exposed two additional compatibility failures: inherited team-worker hooks blocked explicitly granted Studio screens, and MCP Python 2.x renamed the input-schema and error fields expected by native Hermes. The extension now supplies the missing read aliases in its own process, refreshes the managed screen schema fingerprint, and adapts only recognized inherited worker boundaries. Actual screen execution still requires a verified Studio session and an explicit profile grant. Scheduled workers, unrelated tools, unknown policies and revoked screen grants remain restricted. Plugin backups retain distinct filenames and rollback metadata.

After canary promotion, all four tested GTM profiles completed their live screen-tool checks. Team Support took one read-only snapshot of its existing page. GTM, assistant and revenue profiles each navigated to Selenium's public test form, entered a distinct marker, changed the test checkbox and verified a fresh snapshot. Recorded tool results confirmed each marker on its own screen and no other profile's marker. Nothing was submitted or uploaded. Their acknowledgments were 155, 398, 195 and 202 ms; these four observations are not a p95 estimate. Earlier failed tests are retained, including the browser's correct refusal of the internal test URL; that refusal was not bypassed.

The real Mac-to-canary selected-skill path passed the reviewed initial copy, a later one-file update and conflict preservation. The installed destination edit and the incoming version were both verified on disk. No credentials, profile settings or histories were included, and transferred scripts did not execute. Tests used an isolated fixture within existing profiles.

Focused compatibility, screen bridge, updater, permission and coordination checks: 61 tests passed. Transport and tool evidence do not establish Studio browser appearance, visible takeover, sleep/wake reliability or the full 24-hour acceptance window. Those checks remain separately tracked.

Follow-up backend acceptance: an owner takeover paused the test assistant's actual browser tool; resuming restored access to its unchanged marker and checkbox. Four simultaneous authenticated GTM viewers passed again, and 16 later warm reconnections had p95 2,014 ms. One attempted measurement during a planned extension restart correctly reported offline and was excluded from healthy warm timings. No protocol check requested pixels or desktop input.

A temporary directional Mac-to-canary grant passed a real file-only A2A handoff. Acknowledgment took 316 ms, the receiver verified the imported file, a repeated request reused the same receipt, the reverse direction remained forbidden, and revocation removed the tested permission. Only the temporary test grant was revoked. This does not establish cancellation of every in-flight tool effect.

The updated cloud qualification monitor records optional read-only screen ownership states and classified connection failures without persisting raw provider errors. It does not allocate or operate screens. Forty-four cloud tests, TypeScript checks and the production build passed. The next named 24-hour window retains previous trial records and must be reviewed separately from browser acceptance; elapsed time or a healthy status alone cannot mark the full release accepted.

### Public domain hosting — September 14, 2026

The gateway can optionally serve the fixture-only public website on separate public hostnames. It rejects private API, setup and WebSocket access on those hosts, including requests with valid private credentials. The private app and existing connector addresses retain their routes. This changes website delivery only; computer-side Hermes extensions and provider settings are unchanged.

Local verification: 49 cloud tests passed, including five new integration tests for public pages/downloads, canonical redirects, owner-cookie/API isolation, WebSocket refusal, path confinement and forwarded-host spoofing. Cloud TypeScript checks and production build passed. All five public-site fixture tests and its production build passed. These checks do not constitute browser visual acceptance.

DNS administration is now available at the existing registrar. A complete DNS export is retained privately before changes. Domain certificate activation, production delivery and hosted CI are tracked separately until verified; historical pending entries above describe their earlier checkpoints. The repaired-version reliability window continues with its original deadline. Any gateway deployment during that window is recorded as a transition, not a fresh 24-hour qualification of the deployed build.

Final domain checkpoint: the production container for `bd4f405` built successfully and passed its health check; [hosted verification passed](https://github.com/jbellsolutions/revenue-partner-studio/actions/runs/34900438483). All three domain certificates and ownership checks are valid. Anonymous HTTPS delivered `/`, `/demo`, `/build`, `/prep`, both generated assets, the favicon and all three preparation downloads; their bytes matched the reviewed local build. The `www` redirect preserved the path and query. Private login, reopening with the same session, a scoped status operation and five computer event streams passed on the custom app origin. Anonymous private API access returned 401; the public host returned 404 for private APIs and 403 for an authenticated WebSocket attempt. The original app address also remained healthy. No additional paid service or computer was created.

The existing repaired-version backend window runs from September 14 at 19:34:19 UTC until September 15 at 19:34:19 UTC. At the post-deployment checkpoint, four real tool jobs remained verified and all five computers were online. Its deadline and earlier records were preserved, with the gateway domain-routing deployment recorded as a transition. The original administrator browser-policy check still denied public-site navigation; visual, pointer, mobile and browser-reopening acceptance remain unverified. HTTP and transport results do not substitute for those checks.

### Connector-only recovery — September 15, 2026

A separate recovery controller now diagnoses sustained Orgo disconnections and can start only a verified failed Studio connector. The first-time installer remains separate. Paused/stopped access, healthy connectors, unknown ownership, Hermes execution, screens and accepted tasks are preserved. Persisted incidents and remote receipts reconcile uncertain acknowledgments across transports and gateway restarts; starts are bounded to two per ten minutes. Optional SSH configuration is owner-only, encrypted, fingerprint-pinned and tested for restricted command access before activation.

Local checks: 57 cloud tests, cloud TypeScript and production build passed; 20 focused Python tests and eight installation/configuration tests passed. Tests cover durable receipts, ambiguous starts, same-name computer isolation, original-profile history verification, unsafe-state refusal, attempt limits, setup ownership, SSH denial, encrypted settings and revocation. These tests do not establish native SSH availability on a live provider endpoint.

A guarded connector-only canary promotion passed with source/configuration/database backups, authenticated readiness, and the identical Hermes runtime process retained. Its connector reports `connector-recovery-1`; the Hermes extension remains `screens-recovery-2`. Existing Orgo API metadata does not expose native SSH endpoints. Some inspected computers have no SSH daemon; automatic SSH remains disabled until actual endpoint and restriction qualification passes. No image, paid computer or provider was replaced.

The existing trial's deadline is preserved. Connector and gateway promotions are recorded as transitions; neither those transitions nor the existing monitor qualify the new repair controller for an uninterrupted 24 hours. Live rollout verification, a subsequent named qualification window, independent client installation, permitted visual/browser acceptance and live native SSH fallback qualification are tracked separately.
